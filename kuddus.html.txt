<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Neurova AI | Text & QR Generator</title>

<style>
:root{
    --glass: rgba(255,255,255,0.12);
    --dark: rgba(0,0,0,0.35);
    --accent: #00f2fe;
    --accent2:#00ff99;
}

*{
    box-sizing:border-box;
}

body{
    margin:0;
    min-height:100vh;
    display:flex;
    justify-content:center;
    align-items:center;
    background:linear-gradient(135deg,#0f2027,#203a43,#2c5364);
    font-family:"Segoe UI",system-ui;
    color:#fff;
}

/* ===== App Container ===== */
.app{
    width:900px;
    padding:28px;
    border-radius:24px;
    background:var(--glass);
    backdrop-filter:blur(18px);
    box-shadow:0 25px 55px rgba(0,0,0,0.4);
}

/* ===== Header ===== */
.header{
    display:flex;
    align-items:center;
    gap:16px;
    margin-bottom:18px;
}

.logo{
    width:60px;
    height:60px;
    border-radius:16px;
    background:linear-gradient(135deg,#00f2fe,#4facfe);
    display:flex;
    justify-content:center;
    align-items:center;
    font-size:26px;
    font-weight:800;
    color:#000;
    box-shadow:0 0 20px rgba(0,242,254,0.6);
}

.brand h1{
    margin:0;
    font-size:30px;
    letter-spacing:1px;
}

.brand span{
    color:var(--accent);
}

.brand p{
    margin-top:4px;
    font-size:13px;
    opacity:0.75;
}

/* ===== Input ===== */
textarea{
    width:100%;
    min-height:90px;
    padding:14px;
    border-radius:14px;
    border:none;
    outline:none;
    resize:none;
    font-size:15px;
    background:rgba(255,255,255,0.15);
    color:#fff;
}

textarea::placeholder{
    color:rgba(255,255,255,0.6);
}

button{
    width:100%;
    margin-top:12px;
    padding:13px;
    border-radius:14px;
    border:none;
    font-size:15px;
    cursor:pointer;
    font-weight:600;
    background:var(--accent);
    color:#000;
}

/* ===== Preview ===== */
.preview{
    display:flex;
    gap:20px;
    margin-top:22px;
}

.text-box,.qr-box{
    flex:1;
    padding:18px;
    border-radius:18px;
}

.text-box{
    background:var(--dark);
}

.text-box p{
    margin-top:10px;
    line-height:1.5;
    word-break:break-word;
    opacity:0.95;
}

.qr-box{
    background:#fff;
    color:#000;
    text-align:center;
}

.qr-box img{
    max-width:220px;
    margin-top:8px;
    opacity:0;
    transition:0.3s;
}

.download{
    display:inline-block;
    margin-top:12px;
    padding:9px 16px;
    background:var(--accent2);
    color:#000;
    text-decoration:none;
    border-radius:10px;
    font-weight:700;
    pointer-events:none;
    opacity:0.5;
}

footer{
    text-align:center;
    margin-top:18px;
    font-size:13px;
    opacity:0.65;
}
</style>
</head>

<body>

<div class="app">

    <!-- ===== Brand Header ===== -->
    <div class="header">
        <div class="logo">AI</div>
        <div class="brand">
            <h1>Neurova <span>AI</span></h1>
            <p>Think Smart. Build the Future.</p>
        </div>
    </div>

    <!-- ===== Input ===== -->
    <textarea id="qrText" placeholder="Enter your text here..."></textarea>
    <button onclick="generateQR()">Generate QR</button>

    <!-- ===== Preview Section ===== -->
    <div class="preview">

        <div class="text-box">
            <h3>Text Preview</h3>
            <p id="textPreview">Your text will appear here</p>
        </div>

        <div class="qr-box">
            <h3>QR Code</h3>
            <img id="qrImage">
            <br>
            <a id="downloadBtn" class="download" download="qr.png">Download QR</a>
        </div>

    </div>

    <footer>Neurova AI • Simple • Fast • Reliable</footer>

</div>

<script>
function generateQR(){
    const text = document.getElementById("qrText").value.trim();
    if(!text){
        alert("Please enter text");
        return;
    }

    document.getElementById("textPreview").innerText = text;

    const qrURL =
      `https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=${encodeURIComponent(text)}`;

    const img = document.getElementById("qrImage");
    img.src = qrURL;
    img.style.opacity = 1;

    const btn = document.getElementById("downloadBtn");
    btn.href = qrURL;
    btn.style.pointerEvents = "auto";
    btn.style.opacity = 1;
}
</script>

</body>
</html>